# Blocker Listesi

Canlı belge — her fazda güncellenir, kapatılan blockerlar silinmez, "Kapatıldı" olarak işaretlenir.

| # | Blocker | Etkilenen gereksinim | Ne eksik | Kullanıcının yapması gereken | Durum |
|---|---|---|---|---|---|
| 1 | Gerçek Supabase kredensiyali yok | REQ-COMMERCE-006, tüm API rotaları | `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` (kök) — bu konuşmanın önceki turunda MongoDB→Supabase taşıması koddan tamamlandı ama canlı veriyle hiç test edilmedi. **Faz 3'te eklenen `202607230001_kademedia_audit_and_quote_states.sql` da bu listeye dahil** — `logActivity()` `target_type/target_id/before/after` gönderiyor; migration uygulanana kadar bu kolonlar yok, fonksiyon `42703 (undefined_column)` hatasını yakalayıp otomatik olarak temel alan setiyle tekrar dener (bkz. `server/api/_lib/notify.js`), yani log yazımı kesintiye uğramaz — migration uygulanınca zenginleştirilmiş alanlar kod değişikliği gerekmeden otomatik yazılmaya başlar | Vercel'de kök `kademedia` projesine bu iki değişkeni ekleyin (KadeAI'nin zaten kullandığı aynı Supabase projesinden), sonra `apps/kadeai/supabase/migrations/2026072*.sql` dosyalarının TAMAMINI (202607230001 dahil) sırayla uygulayın | Açık |
| 2 | Hukuki metinler avukat onayı bekliyor | REQ-LEGAL-002 | KVKK, mesafeli satış, tüketici koruma, e-ticaret kanunu uyumlu **son metin** — mevcut `/kvkk`, `/gizlilik`, `/cerez-politikasi`, `/telif-haklari` sayfalarının içeriği teknik altyapı olarak var ama hukuken bağlayıcı nihai metin değil | Yetkin bir Türk hukukçusuna (tercihen e-ticaret/KVKK deneyimli) mevcut metinleri incelettirin | Açık |
| 3 | Onaylı ticari fiyat verisi yok | REQ-LAUNCH-001, REQ-COMMERCE-002/003 | Legacy `/paketler` için gerçek, güncel, satışa çıkarılabilir TRY/USD fiyatları — admin panelinden girilebilir hale getirildi (bu oturumda) ama şu an boş | Gerçek fiyatları admin panelindeki "Paketler" sekmesinden girin | Açık (altyapı hazır) |
| 4 | Doğrulanamayan pazarlama iddiaları (eski sürümde bulundu) | REQ-LEGACY-001, §7/§35 | "Bu ay yalnızca 3 müşteri yerimiz kaldı" (sahte kıtlık), "yüzlerce marka", "3+/4+/20+" istatistikleri — kaynağı/güncelliği doğrulanamadı, bkz. `docs/LEGACY_PAGE_GAP_ANALYSIS_TR.md` | Bu rakamlar gerçekse güncel ve doğrulanabilir haliyle iletin; değilse bu ifadeler kullanılmayacak (varsayılan: kullanılmayacak) | Açık |
| 5 | AI görünürlük programı (Ek A) için gerekli erişim yok | REQ-SEO-001…017 | ChatGPT/Claude/Gemini/Grok/Perplexity'de 150+ sorgu testi için resmi API erişimi veya kontrollü hesap erişimi; Google Search Console, Bing Webmaster, Google Business Profile doğrulaması için site/işletme sahibi erişimi | Mümkünse API anahtarları/hesap erişimi sağlayın; sağlanamıyorsa bu belgeler "kullanıcı erişimi bekliyor" olarak işaretli kalacak, tahmini/uydurma veriyle doldurulmayacak | Açık |
| 6 | PR #8 kapatma işlemi yapılamadı | Vercel mimari kararı | `gh` CLI bu ortamda kurulu değil, PR'ı programatik kapatamadım | `https://github.com/kadirdemirs/kademedia/pull/8` adresinden manuel kapatın (önceki oturumun gerekçesi: geçersiz Vercel "services" şeması, bkz. `VERCEL_SERVICES_MIGRATION_REPORT_TR.md`) | Açık |
| 7 | Paket yöneticisi belirsizliği | REQ-CODE-001 | Kökte `bun.lock`, `package-lock.json`, `pnpm-lock.yaml` üçü birden var, hangisi kaynak belirsiz | Hangi paket yöneticisinin kullanılacağına karar verin (npm önerilir, bu oturumda tutarlı çalıştı), diğer lock dosyaları kaldırılabilir | Açık, düşük öncelik |
| 8 | Kade Studio ile §17 (ChatCut alternatifi) ilişkisi net değil | REQ-VIDEO-001 | `apps/studio-web`/`apps/studio-worker` zaten bir video editör ürünü (Drizzle/Redis/BullMQ/MinIO ile); `apps/kadeai`'de de `video-factory`/`clip-generator`/`dubbing` sayfaları var. Şartname §17 "yeni" bir video editör istiyor — üç farklı video ürünü aynı anda inşa edilmemeli | Kade Studio'nun kapsamını ve şartname §17 ile ilişkisini netleştirin: aynı ürün mü, tamamlanmamış mı, terk mi edildi? | Açık, Faz 6 öncesi netleşmeli |
| 9 | `apps/kadeai` mevcut araçları ile §15/§16 örtüşmesi netleşmedi | REQ-TOOLS-002/003 | `apps/kadeai/app/dashboard/**` altında hook/title/hashtag/viral-score/competitor/social-audit/retention-analysis gibi 20+ araç zaten var — şartnamedeki "oluştur" listesiyle büyük örtüşme var, bkz. `docs/03_ROUTE_INVENTORY_TR.md` | Faz 5 başlamadan önce madde-madde gap analizi yapılmalı (bu turun kapsamı dışında bırakıldı) | Açık, Faz 5 öncesi netleşmeli |
| 10 | Eşzamanlı ikinci bir oturum (worktree) tespit edildi | Genel koordinasyon | `../kademedia-pr8-hardening` klasöründe `codex/pr8-hardening` dalında ayrı bir ajan/oturum aktif olabilir; ayrıca ana çalışma dizininde commit edilmemiş 3 dosya (`PricingPanel.tsx`, `pricingOverrides.ts`, `pricing-overrides.test.ts`) başka bir oturuma ait görünüyor, silinmedi/değiştirilmedi | Aynı anda birden fazla ajan/oturum çalıştırıyorsanız çakışma riskine karşı koordinasyon önerilir | Bilgi amaçlı, engelleyici değil |
| 11 | E2E test altyapısı yok | REQ-CODE-010 | Playwright route, auth-boundary ve mobil taşma testleri kuruldu; public rota seti yeni altı sayfayla genişletildi | `npm.cmd run legacy:test:e2e`: 70/70 geçti | Kapatıldı |
| 12 | Kök API'ler canlı Supabase'e karşı hiç test edilmedi | REQ-COMMERCE-006 | Bu oturumda yapılan MongoDB→Supabase taşıması yalnızca `node --check` (syntax) ve yapısal doğrulamayla kontrol edildi — gerçek sorgu davranışı doğrulanmadı (blocker #1 ile aynı kök neden) | Blocker #1 çözülünce staging'de tam regresyon | Açık |
| 13 | Marka `sameAs` / sosyal profil URL'leri boş | REQ-SEO-000 (entity/knowledge-graph sinyalleri), Ek A | `src/config/brand.js` içindeki `social: {}` ve `SOCIAL_LINKS = []` kasıtlı olarak boş bırakılmış (kod, doğrulanmamış profil eklememek için doğru davranıyor) — bu yüzden `StructuredData.jsx`'teki `OrganizationSchema()` şu an `sameAs` alanını hiç üretmiyor, bu da Google/AI motorlarının marka varlığını (entity) doğrulamasını zayıflatıyor | Kade New Media'nın gerçek, aktif Instagram/LinkedIn/YouTube/X vb. hesap URL'lerini iletin; `BRAND.social` içine eklenince `sameAs` otomatik oluşur (kod tarafında ek iş gerekmiyor) | Açık, düşük efor/yüksek etki — sadece veri bekliyor |

| 14 | Kupon sistemi ödeme akışına kablolanmadı | REQ-ADMIN-005, REQ-COMMERCE-006 | `kade_coupons` tablosu + admin CRUD + saf doğrulama mantığı (`_lib/coupons.js`) hazır ama `server/api/shopier.js`'teki `validateShopierPayment()` kupon indirimini uygulamıyor — bilinçli olarak ertelendi çünkü ödeme tutarı doğrulaması canlı webhook testi olmadan güvenle değiştirilemez | Blocker #1 çözülüp canlı ortamda test edilebildiğinde: `validateShopierPayment()`'a kupon parametresi eklenip `applyCouponDiscount()` ile indirimli tutar hesaplanmalı, `used_count` atomik artırılmalı | Açık, düşük risk (yalnızca admin CRUD canlı, ödeme etkilenmiyor) |

| 16 | Upstash Redis (kalıcı rate limit) canlıda yapılandırılmamış olabilir | REQ-SEC-001 | `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` `.env.example`'da belgeli ama gerçekten provizyon edilip edilmediği doğrulanmadı. Yapılandırılmazsa `_lib/rateLimit.js` bellek-içi sayaca düşer — Vercel'de fonksiyon örnekleri arası paylaşılmadığı için dağıtık brute-force koruması zayıflar (kod kendisi doğru tasarlanmış, bkz. `docs/07_SECURITY_AUDIT_TR.md`) | Bir Upstash Redis instance'ı oluşturup bu iki env değişkenini Vercel'e ekleyin | Açık, orta öncelik |
| 15 | `apps/kadeai` tarafında da Shopier iade durumu ele alınmıyor | REQ-COMMERCE-006 | Kök tarafında bu turda çözüldü (bkz. kapatılan blockerlar) ama `apps/kadeai/lib/payments/shopierProvider.ts`/`entitlements` tablosu aynı boşluğu taşıyor — iki sistem kasıtlı olarak ayrı tutulduğu için (docs/02 karar #3) kadeai tarafında ayrıca ele alınmalı | Faz 3/5 devamında kadeai'de aynı desenle (manuel admin iade işaretleme + entitlement iptali) ele alınmalı | Açık, orta öncelik |

| 17 | Mesafeli satış hukuki sayfaları hiç yok ama satış zaten canlı | REQ-LEGAL-002 | Shopier ile gerçek ödeme akışı çalışıyor ama Mesafeli Satış Sözleşmesi, Ön Bilgilendirme Formu ve Cayma/İptal/İade Politikası sayfaları hiç yok (bkz. `docs/08_LEGAL_COMPLIANCE_CHECKLIST_TR.md` §2) — 6502 Tüketici Koruma Kanunu ve Mesafeli Sözleşmeler Yönetmeliği açısından somut bir hukuki risk | Hukukçuya bu üç belgeyi öncelikli olarak yazdırın; içerik gelince sayfa/route eklemek düşük efor | Açık, **en yüksek öncelikli hukuki blocker** |

| 18 | Ana sayfa (`/`) konsola hydration hatası basıyor | REQ-SEO-000, REQ-CODE-010 | Eski `public/site.html` donmuş export'unda React #418 vardı | `5516bc4` ile ana sayfa native React'e taşındı; WebGL hero `public/hero.html` iframe'ine izole edildi | Kapatıldı |

| 19 | Kade AI gerçek hesap/Supabase proje eşleşmesi statik olarak doğrulanamıyor | REQ-SEC-003 | Kod env adlarını, URL biçimini ve placeholder değerleri doğruluyor; fakat gerçek Vercel anahtarlarının hangi Supabase projesine ait olduğunu ve giriş e-postasının o projede bulunduğunu repo içinden kanıtlayamaz | `docs/KADEAI_AUTH_DIAGNOSTIC_TR.md` içindeki Dashboard kontrolünü uygulayın: Project URL + anon/publishable key eşleşmesi, Authentication → Users ve callback URL; ardından yeni production deploy oluşturun | Açık, dış sistem doğrulaması |

| 20 | Studio entegrasyon testinin kullanacağı PostgreSQL çalışmıyor | REQ-CODE-010 | `npm.cmd run verify` içindeki 13 editör unit testi geçti; entegrasyon aşaması `localhost:5432` için `ECONNREFUSED` ile durdu. Test kurulumu `projects` tablosunu `TRUNCATE ... CASCADE` ettiği için bilinmeyen/kalıcı bir veritabanını otomatik başlatmak veya bağlamak güvenli değil | İzole, silinebilir bir test PostgreSQL veritabanı sağlayın ve test env değerlerini ona yönlendirin; ardından `npm.cmd run test:integration` ve tam `verify` komutunu yeniden çalıştırın | Açık, test altyapısı |

## Kapatılan blockerlar (bu oturumda)

| # | Blocker | Nasıl kapatıldı |
|---|---|---|
| — | `apps/kadeai` production build ağ kısıtı nedeniyle önceki oturumda doğrulanamamıştı | Bu oturumda ağ erişimi vardı, `npm run build` başarıyla tamamlandı (bkz. `docs/01_CURRENT_STATE_AUDIT_TR.md`) |
| — | 26 Haziran sürümü bulunamıyor/belirsizdi | Üç aşamalı arama ile kesin olarak bulundu ve izole çalıştırıldı (bkz. `docs/LEGACY_PAGE_GAP_ANALYSIS_TR.md`) |
| — | `feat/site-dark-editorial` dalı gerçek build ile doğrulanmamıştı | 24 Temmuz 2026'da aktif dalda `npm.cmd run legacy:build` geçti; 552 modül ve 37 statik rota entry'si üretildi |
| 11 | E2E test altyapısı yoktu | Playwright desktop/mobile projeleri, route smoke, auth boundary ve yatay taşma kontrolleri çalışıyor; son koşuda 70/70 test geçti |
| — | Route envanterinde altı public sayfa eksikti | `/fiyat-hesaplama`, `/basin`, `/neden-biz`, `/referans-programi`, `/podcast-webinar` ve `/bulten-arsivi` CMS, SEO, sitemap, footer ve statik HTML üretimine bağlandı; route denetimi 169/169 |
| — | Ana sayfa hydration hatasının kaynağı çözülemiyordu | `5516bc4` ile ana sayfa native React'e taşındı; donmuş WebGL hero `hero.html` iframe'ine izole edildi. Eski blocker #18 tarihçe için üstte tutuldu ancak aktif release blocker değildir |
| — | Shopier iade/chargeback durumu hiçbir sistemde ele alınmıyordu (docs/05'in Faz 3 öncelik #1'i) | İncelendi: Shopier'ın herkese açık API'si otomatik iade webhook'u göndermiyor (yalnızca status=1 ödeme-başarılı callback'i var, kod incelemesiyle doğrulandı). Bu yüzden gerçekçi çözüm otomatik değil, **admin tarafından manuel iade işaretleme**. `kade_shopier_orders.state`'e `refunded/partially_refunded` eklendi (migration, uygulanmadı), `server/api/shopier.js`'e admin-only `?action=orders` (liste) ve `?action=refund` (iade işaretle + bağlı paketi pasifleştir) eklendi, yeni "Ödeme Kayıtları" admin ekranı eklendi. `apps/kadeai` tarafında (`shopierProvider.ts`) aynı boşluk hâlâ açık — iki sistem kasıtlı olarak birleştirilmediği için (bkz. docs/02 karar #3) orada ayrıca ele alınmalı |
